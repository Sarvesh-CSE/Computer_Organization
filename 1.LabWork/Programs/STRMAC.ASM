TITLE "MACRO IN LIBRARY"

INCLUDE C:\B2\MAC.LBY

DATASEG SEGMENT 
  MSG DB,0AH,0DH,'ENTER A CHARACTER:$'
  STRG1 DB,0AH,0DH,'THE ENTERED CHARACTER IS:$'
  TEMP DB ?
DATASEG ENDS

CODESEG SEGMENT 
  ASSUME CS:CODESEG,DS:DATASEG

START:
  INIT
  PRINT MSG

  MOV AH,01H
  INT 21H
  MOV TEMP,AL

  PRINT STRG1

  MOV DL,TEMP
  MOV AH,02H
  INT 21H

  FINISH

CODESEG ENDS
END START
END